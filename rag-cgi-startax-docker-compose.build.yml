version: '3.8'

services:
  rag-cgi-api:
    build:
      context: .
      dockerfile: Dockerfile
      # Arguments de build si nécessaire
      args:
        - BUILDKIT_INLINE_CACHE=1
    image: rag-cgi-api:latest